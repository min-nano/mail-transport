# deploy/*/*.sh が読み込む設定。deploy/config.env にコピーして書き換える。

PROJECT_ID="my-gcp-project"

# iCloud のメールアドレス
ICLOUD_USERNAME="you@icloud.com"

# VM にアタッチする実行用サービスアカウント
RUNTIME_SA_NAME="mail-transport-run"

# none: 稼働開始以降に届いたメールのみ転送 / all: 既存メールもすべて転送
INITIAL_IMPORT="none"

# 転送が済んだメールを iCloud のゴミ箱へ移すか (iCloud の容量を空けるため)。
# false にすると iCloud 側には手を触れない。
# iCloud はゴミ箱を 30 日で空にするので、それ以降は Gmail が唯一のコピーになる。
TRASH_AFTER_FORWARD="true"

# ゴミ箱へ移す前に Gmail 側を読み戻して確認する。
# auto: 確認できるなら確認する / true: 必ず確認する / false: 確認しない
VERIFY_BEFORE_TRASH="auto"

# INITIAL_IMPORT="all" のとき、稼働開始前からあったメールもゴミ箱へ移すか。
# false なら転送だけ行い、iCloud の受信トレイはそのまま残す。
# 取り込みを始める前に決めること (済んだあとの変更は既存メールに効かない)。
TRASH_EXISTING_ON_INITIAL_IMPORT="false"

# ===== GCE e2-micro (Always Free) =====
# 無料枠の条件 (2026-09 時点の公式情報):
#   - マシンタイプは e2-micro のみ
#   - リージョンは us-west1 / us-central1 / us-east1 のみ
#   - 標準永続ディスク (pd-standard) 30GB まで
#   - 北米からの下り (外向き) 通信 1GB/月まで (中国・オーストラリア宛を除く)
VM_NAME="mail-transport"
VM_ZONE="us-west1-b"
VM_MACHINE_TYPE="e2-micro"     # 変更すると無料枠から外れる
VM_DISK_SIZE_GB="30"           # 無料枠の上限
VM_DISK_TYPE="pd-standard"     # コンソール既定の pd-balanced は無料枠の対象外
VM_IMAGE_FAMILY="debian-12"
VM_IMAGE_PROJECT="debian-cloud"

# ===== CI (GitHub Actions) からデプロイする場合 =====
# deploy/ci/00-setup-wif.sh がこのリポジトリからの実行だけを信頼するよう設定する。
GITHUB_REPO="min-nano/mail-transport"
